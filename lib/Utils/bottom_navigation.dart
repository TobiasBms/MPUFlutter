import 'package:flutter/material.dart';

class TMABottomNavigation extends StatelessWidget {
  const TMABottomNavigation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
